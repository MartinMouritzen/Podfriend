import React from 'react';

const QuickMessagePopup = () => {
	return ReactDOM.createPortal(
		<div>
			Hell yeah. Jeg er gennemsigtig tekst på sort baggrund med opacity og blur
		</div>
	);
};
export default QuickMessagePopup;